# Server environment example
PORT=5000
MONGO_URI=
JWT_SECRET=
JWT_EXPIRES_IN=7d
# Comma-separated list of allowed origins for CORS. Include deployed frontend URLs.
# e.g. http://localhost:5173,https://your-vercel-app.vercel.app,https://username.github.io
CORS_ORIGIN=http://localhost:5173
